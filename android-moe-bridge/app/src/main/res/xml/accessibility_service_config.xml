<?xml version="1.0" encoding="utf-8"?>
<!--
  packageNames is intentionally NOT set here so the service receives events from
  ALL apps; the service filters to the configured MOE package(s) in code (see
  Prefs / MoeAccessibilityService). This enables the "capture all" debug mode used
  to DISCOVER the real MOE Parents Gateway package name on-device.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeNotificationStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagIncludeNotImportantViews|flagReportViewIds|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_description"
    android:notificationTimeout="200" />
